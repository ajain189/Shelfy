# Copy this file to `.env` and fill in your real key.
# `.env` is gitignored and never committed.
#
# Get a free Gemini API key at https://aistudio.google.com/apikey
#
# DEMO-ONLY: this key is bundled into the app so Expo Go can call Gemini directly.
# Production would route through a backend proxy instead (see app.config.ts).
GEMINI_API_KEY=AIza...
